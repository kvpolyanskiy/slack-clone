subscription MessageAdded($channelId: String!) {
  messageAdded(channelId: $channelId) {
    id
    text
    createdDate
    channelId
    createdDate
    user {
      userId
      username
      avatar
    }
  }
}
